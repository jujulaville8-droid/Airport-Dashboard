export type RiskClass = 'CRITICAL' | 'AT_RISK' | 'HEALTHY' | 'DEAD_STOCK' | 'OVERSTOCKED';
export type InventoryItem = { itemNo: string; descr: string; category: string | null; qtyOnHand: number; unitCost: number; totalValue: number; velocityPerDay: number; daysOfCover: number | null; lastSaleDate: string | null; risk: RiskClass; reorder: { min: number | null; reorderPoint: number | null; max: number | null; leadTimeDays: number } };
export type InventoryRisk = { asOf: string; snapshotDate: string | null; salesWindow: number; updatedAt: string | null; critical: InventoryItem[]; atRisk: InventoryItem[]; healthy: InventoryItem[]; deadStock: InventoryItem[]; overstocked: InventoryItem[]; summary: { criticalCount: number; atRiskCount: number; deadStockCount: number; deadStockValue: number; overstockedCount: number; totalSkusTracked: number; totalInventoryValue: number; snapshotDate: string | null } };
export type ReorderRule = { minStock: number; reorderPoint: number; maxStock: number; leadTimeDays: number };
